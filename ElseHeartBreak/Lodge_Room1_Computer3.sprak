#ExportMark
# Test isStrInStr

var MC = Connect("Computer1")
string s1 = "There can be no mistake, this is a string."
string s2 = "take"
string s3 = "can"
string s4 = "Hey, no way!"

if MC.isStrInStr(s2, s1)
    Print("Test1 passed.")
end

if MC.isStrInStr(s3, s1)
    Print("Test2 passed.")
end

if MC.isStrInStr(s4, s1)
    Print("Test3 passed.")
en